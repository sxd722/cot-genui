# LLM 配置（兼容 OpenAI 接口的任意端点）
# 不填也可运行：前端会使用 mock 输出调试 UI

# API Key（必填才走真实推理）
LLM_API_KEY=

# Base URL（可选，指向 GLM / DeepSeek / Azure 等兼容端点）
# 例: https://open.bigmodel.cn/api/paas/v4
LLM_BASE_URL=

# 模型名（可选，默认 gpt-4o-mini）
LLM_MODEL=gpt-4o-mini
