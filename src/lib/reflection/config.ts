import type { ModelProfile } from "@/lib/pipelineTypes";

/** Reflection handles the longest compact episode views and always uses GLM Thinking. */
export const REFLECTION_MODEL_PROFILE: ModelProfile = "glm_5_2_thinking";

